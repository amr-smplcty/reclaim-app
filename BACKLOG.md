# BACKLOG.md — Deferred items (fine as-is now, must be addressed later)

> Running list maintained across epics. Items get a status, a source, and a "due before" gate.
> Gates: **E3** = during Epic 3 · **TF** = before TestFlight · **L** = before public launch · **P** = post-launch polish.

## 🔴 Launch-critical (hard blockers at their gate)

| # | Item | Source | Gate |
|---|------|--------|------|
| 2 | Attorney review of legal/tou.md + legal/privacy.md (both marked DRAFT); tailor arbitration + state notices; confirm DBA/trade-name need for "Reclaim"/"Smplcty Analytics" under Simplifico LLC | Epic 2b / LEGAL_COMPLIANCE §4–8 | L |
| 3 | Clinician review batch (CLINICAL_SPEC §8): Weeks 1–6 lesson/exercise copy, Urge Surf script, lapse-debrief emotion chips, urge trigger chips, onboarding intake options, crisis_patterns.json expansion | Epics 2/4/5 reports | L |
| 4 | Record (or TTS-generate) the Urge Surf guided audio from the authored narration script (content/week2.json `toolkit_scripts.urge_surf.narration_script`); drop the file's URL into `toolkit_scripts.urge_surf.audio_url`. The playback integration point now exists (`src/lib/audio/guidedAudio.ts`, expo-audio, INC-2 graceful) and is wired into Urge Surf — it plays automatically once `audio_url` is present, no code change needed; this item is now just the content (audio file) | Epic 5 TODO(content) / Week 2 pack | L |
| 5 | App icon/splash are still placeholder assets — replace with real designed art before any public build. (Bundle ID half resolved: `com.smplctyanalytics.reclaim` confirmed and set in app.json/EAS, Epic 3) | Epic 1 report | TF |
| 6 | Apple Developer organization enrollment as Simplifico LLC (pending D-U-N-S) → unblocks Epic 3, TestFlight | Business track | E3 |
| 62 | **Upgrade Supabase off the free tier before real users exist.** Free-tier projects auto-pause after ~1 week idle (see INCIDENTS.md INC-17 — it silently broke the delete-account deploy). In production this would break auth, sync, and Edge Functions (including delete-account) for real users with confusing errors until someone manually restored it — unacceptable for a paid app. Move to a paid plan (no auto-pause) before launch | fix-40 deploy / INC-17 | L |

## 🟡 Engineering debt (correct behavior exists; wiring or robustness pending)

| # | Item | Source | Gate |
|---|------|--------|------|
| 7 | Email/OTP sign-in path: legal acceptance stored locally but not remote-synced until session exists — close when real session/deep-link handling is built | Epic 2b report | TF |
| 8 | legal/*.md and src/lib/legal/content.ts are hand-duplicated — automate sync or add a CI check that they match (critical once attorney edits land) | Epic 2b report | L |
| 9 | Supabase project credentials in .env (if not yet created) + first real auth/sync test end-to-end | Setup step | TF |
| 10 | Content currently bundled at build time; move to Supabase-served content packs + offline cache (MMKV) once hot-updating matters | Epic 4 report | P |
| 13 | 10-Minute Shift: add dev-build-only fast-timer toggle (15s) for manual testing of full flow incl. escalation; impossible to trigger in production | Epic 5 review | TF |
| 15 | Native module MMKV is installed but unwired — requires the EAS dev client (now real, Epic 3) to actually exercise. (RevenueCat wired in Epic 3; `@shopify/react-native-skia`, `victory-native`, and `@expo/ui` were installed-but-unused and removed entirely in fix-expo-av-sdk57 to shrink native build surface — charts are hand-built react-native-svg per CLAUDE.md, so nothing depended on them) | Epic 1 report | E3 |
| 26 | Checklist_commit next-day follow-up only checks the position immediately before current — if a user completes multiple days without opening Today in between, an eligible follow-up (e.g. W2D4's) can be skipped entirely. Should scan all unanswered eligible follow-ups, not just the last one. (Epic 11: the "look at yesterday" logic itself was extracted into a tested pure function, `resolveChecklistFollowup`, and confirmed to correctly handle multiple *independent* instances (W2D4, W5D2) — this item is about scanning further back than one day, still open) | Epic 5b report | Epic 7 |
| 28 | Journal encryption-at-rest (AES via crypto-js, key in expo-secure-store) covers only the new check-in store (useJournalStore). Urge logs and lapse debriefs (toolkit store) and lesson reflections (program store) — all of which the Journal timeline displays — remain on plain unencrypted AsyncStorage. Consider extending the same encrypted-storage adapter to those stores in a hardening pass | Epic 6 report | TF |
| 29 | Legacy check-in migration (useJournalStore.migrateLegacyCheckins) runs on Journal tab mount, not on app launch — if a user never opens Journal, any pre-Epic-6 check-in data sits unmigrated (harmless, just not yet visible in the timeline). Fine given no real users exist yet; revisit if that's no longer true | Epic 6 report | TF |
| 31 | Account screen's "Continue without account (dev)" bypass (`__DEV__`-gated, throws outside dev builds, never calls Supabase) exists purely so QA can get past Apple/email sign-in in Expo Go — re-verify it's actually unreachable in a real EAS build before TestFlight | Epic 2c report | TF |
| 32 | Root cause of the results-screen crash fix: every onboarding screen stays mounted for the whole flow (goNextFrom uses router.push, never replace/pop), so any screen that derives something render-time from useOnboardingStore is exposed to a reactive re-render if the store changes while it's parked off-screen. Fixed the concrete case (ResultsScreen) with a validity guard, but the underlying pattern — the entire onboarding stack staying mounted start to finish — is still there and could bite the next validation-heavy screen. Consider popping/replacing as steps complete, or a doesn't-crash-on-invalid-state lint/convention | fix-results-scoring-crash report | Epic 3 or a hardening pass |
| 33 | `npm run lint` (expo lint / ESLint) has never actually been run as part of this project's verification loop — only the custom `lint:banned-words`/`lint:tokens`/`lint:bundle-purity` scripts have. Running it for the first time (investigating the testlib-leak bug) auto-installed `eslint`/`eslint-config-expo` and surfaced 57 pre-existing problems across many files from earlier epics (mostly `react/no-unescaped-entities` on apostrophes in JSX text, and `@typescript-eslint/array-type` preferring `T[]` over `Array<T>`) — none related to that bug, so the auto-installed eslint config was reverted rather than fixing all of them in scope. Worth either adopting ESLint properly (fix the backlog, wire it into the verification loop) or deciding it's not needed | fix-testlib-leak report | hardening pass |
| 34 | Commitment Goal daily-credit reconciliation (Epic 7) only catches up "yesterday" each time the Progress tab is opened — not a full historical backfill. If a user doesn't open Progress for several days in a row, the skipped days' credit is never retroactively computed (their goal just progresses more slowly than it should have, never incorrectly — no data is lost, `lastCreditedDateKey` just moves forward without back-filling the gap) | Epic 7 report | hardening pass |
| 36 | Several Epic 7 numeric thresholds are reasonable defaults chosen without an exact CLINICAL_SPEC number, worth a founder/clinician sanity check before launch: lapse-delay cap (total delay capped at the goal tier's own day-length), day-credit weighting (lesson/check-in/clean-day each 1/3, lapse-with-debrief = 1/6), pattern-insight dominance threshold (50%, on top of §7's explicit ≥5-entries minimum), and growth-visual stage thresholds (8 points per stage, 6 stages total) | Epic 7 report | before clinician review (item #3) |
| 38 | Testing convention discovered in Epic 7: in this project's React 19 + react-test-renderer setup, two interactive RNTL tests in the *same file* corrupt each other once one of them swaps a screen's conditional root view (e.g. a form → result-view transition) — confirmed with a minimal, code-independent repro. Workaround: one interactive test per file when a screen has this shape (see `__tests__/modals/reassessment-submit.test.tsx` / `reassessment-delta.test.tsx`). Also: `@testing-library/react-native` auto-registers its own awaited `afterEach(cleanup)` on import — never call `cleanup()` again manually in a test file, it races with the auto one and corrupts the next test. **Epic 10 refinement:** the same corruption can happen from *two separate `render()` calls inside one `it()`* if the first render's own conditional root view gets swapped (e.g. its "Save" button pressed) before the second `render()` call — keep at most one root-view-swapping interaction per test *file*, not just per `it()` (see `__tests__/toolkit/log-urge-value-tag.test.tsx`, where the fix was to make the first render query-only) | Epic 7 report / Epic 10 report | testing convention, no gate |
| 39 | content/week3.json's Day 4 guided_list payload carries an extra `companion_tool: "defusion"` field not part of any existing payload contract — not wired to anything (GuidedList doesn't read it). Likely intended to deep-link into the Defusion tool after building the list of recurring thoughts; left unbuilt since it wasn't in this reconciliation's explicit scope | Epic 8 reconciliation report | next content-integration pass |
| 40 | **Code complete (fix-40-delete-edge-function); needs one deploy.** `delete-account` Edge Function (`supabase/functions/delete-account/`) authenticates the caller from their JWT and deletes the `auth.users` row via the service-role key (auto-injected server-side, never in the app/.env). The app invokes it before sign-out (`deleteAccountRemotely`), gracefully handling the not-yet-deployed case (rows still deleted client-side, session ended, local data wiped, gap logged). **Remaining:** run `supabase functions deploy delete-account` once (walkthrough delivered in the Task C session) — until then the auth.users row still lingers in production | Epic 9 report | before real Supabase project (BACKLOG #9) / TF |
| 41 | App-lock grace period (30s, `src/features/lock/lockPolicy.ts`'s `APP_LOCK_GRACE_PERIOD_MS`) is an engineering-chosen default, not a spec number — same category as BACKLOG #36's Epic 7 thresholds. Worth a founder sanity check: too short annoys users switching apps briefly, too long weakens the "privacy is a feature" promise | Epic 9 report | before clinician review (item #3) |
| 42 | Notification/time-picker UI (`TimeStepperRow`) is a hand-rolled hour/minute stepper, not a native wheel picker. The dev client now exists and the DOB picker was migrated to `@react-native-community/datetimepicker` (fix-device-qa-1) — the same package's `mode="time"` could replace TimeStepperRow the same way (INC-2 availability-checked, stepper as fallback). Left for a follow-up since notification times weren't in the QA batch | Epic 9 report | E3/TF |
| 43 | Discreet app icon (PRODUCT_SPEC §5.6, "privacy is a feature") not built — alternate app icons need native config (`expo-alternate-app-icons` or Expo's own `expo-build-properties`/`CFBundleIcons` approach) that Expo Go can't run; needs a dev client like the other items in this gate. Deliberately deferred rather than half-built in Epic 9 | Epic 9 report | E3+ dev client era |
| 44 | Week 4 Day 4's `valueTag` (urge_value_map's `enable_ongoing_tagging`) is captured on every real urge log going forward (`UrgeLogEntry.valueTag`) but not yet analyzed anywhere — content/week4.json's own notes say this "feeds Progress pattern insights later." `patternInsights.ts` still only computes the time-of-day cluster; a "which value is your urges' most common ask" insight is unbuilt | Epic 10 report | next patterns/insights pass |
| 45 | `value_card_sort`'s top-5 ranking step is tap-to-order (numbered badges, tap again to un-rank), not drag-and-drop — a deliberate choice to avoid adding a gesture/reorder-list dependency for one screen. Revisit if a future card-sort-style exercise needs true drag reordering | Epic 10 report | P |
| 48 | `RiskWindowPlanner`'s per-window plant-option chip row has no display cap — `resolvePlantOptions` returns every unique item across movement_plan/reconnection_plan/boredom_plan/shift_list with no limit, so a user with a long boredom_plan sees a long horizontally-scrolling chip row. Harmless (horizontal ScrollView), but worth a sanity check once real content/usage exists | Epic 11 report | P |
| 49 | `RiskWindowPlanner`'s `allow_free_text: false` path (chip-only, no override text field) is implemented but untested — every real payload today (content/week5.json Day 6) sets it `true`. Add coverage if a future week ever sets it `false` | Epic 11 report | next content-integration pass |
| 51 | Refresher week's "redo the exercise" deep-link writes to the SAME real `save_to` key as the original Week 2/3 day — a deliberate "tune-up updates your plan" design choice (not reviewed against CLINICAL_SPEC by a clinician), not a separate namespaced copy. Worth a founder/clinician sanity check: is silently overwriting the user's original historical Week 2/3 answers the right call, or should a refresher redo be visibly versioned/kept alongside the original | Epic 12 report | before clinician review (item #3) |
| 52 | Maintenance-mode check-in cadence → weekday mapping (`cadenceToWeekdays` in `src/lib/notifications/schedule.ts`): "3 evenings a week" → Mon/Wed/Fri and "Weekly" → Sunday are engineering-chosen defaults, not a spec number — same category as BACKLOG #36/#41. Worth a founder sanity check before launch | Epic 13 report | before clinician review (item #3) |
| 53 | Risky-window reminder's "modal hour" (PRODUCT_SPEC §7) is approximated as the dominant time-window's start hour minus 30 minutes (`riskyWindowReminderTime`) — `patternInsights.findDominantTimeWindow` only reports a 4-6 hour bucket, not one exact clock hour, so there's no true single "modal hour" to read yet. Revisit if pattern insights ever compute a real peak-hour, not just a dominant bucket | Epic 13 report | next patterns/insights pass |
| 54 | Dev-only day-fast-forward control (`fastForwardCurrentDay`, Today tab, `__DEV__`-gated) saves a generic placeholder object under the day's real `save_to` key rather than a type-correct payload shape — good enough to unstick program-position advancement for a founder QA speed-run, but downstream screens that read a specific exercise's shape (Emergency Card, profile builders, `summarizeExerciseOutput` consumers) will show blank/placeholder sections for any fast-forwarded day. Same `__DEV__`-unreachability re-check as BACKLOG #31 applies before TestFlight | Epic 13 report | TF |
| 60 | `useSubscriptionStore.purchase`'s `trial_started` vs `subscription_started{plan}` analytics split is inferred client-side from the resulting entitlement's `periodType === 'TRIAL'` — the only trial signal observable at the moment of purchase (a later trial→paid conversion happens server-side via RevenueCat webhooks, not something the client purchase call can see). Not verified against a real sandbox trial purchase yet (no device/EAS build available in this session) — confirm the real periodType value during first sandbox test | Epic 3 report | TF |
| 61 | Paywall's "save X%" framing (`annualSavingsPercent`) and the trial/price copy are computed live from RevenueCat's fetched offering rather than hardcoded — correct by construction, but never exercised against the *real* App Store Connect products yet (no device/EAS build available in this session, all coverage is mocked-SDK unit tests). Confirm during first sandbox test that the real fetched prices render exactly as expected | Epic 3 report | TF |

## 🟢 UX polish (works, but rough)

| # | Item | Source | Gate |
|---|------|--------|------|
| 18 | Manually run the real 10-minute Shift timer to completion once on device (founder QA) | Epic 5 review | TF |
| 19 | Full manual QA pass of Week 1 content threading on device (D2/D3 → D4; D1/D4/D6 → D7 commitment; crisis guard; relaunch-resume) | Epic 4 review | ongoing |

## 🔵 Business track (not code)

| # | Item | Source | Gate |
|---|------|--------|------|
| 20 | RevenueCat account creation (company email) + project setup, so Epic 3 starts instantly when Apple clears | Wait-time plan | E3 |
| 21 | Name check: App Store search for "Reclaim" in health/wellness, domain availability, USPTO trademark search — before brand hardens | Wait-time plan | L |
| 22 | Clinician recruitment (licensed, behavioral-addiction familiar, $300–500 review) — lead time now, review after Week 6 content exists | Wait-time plan | L |
| 23 | Attorney shortlist (2–3 app/health-tech lawyers) — engage when legal drafts final | Wait-time plan | L |
| 24 | Substantiation folder: PDFs of every cited study + claims-to-source mapping sheet | LEGAL_COMPLIANCE §8 | L |
| 25 | Tech E&O / general liability insurance quotes | LEGAL_COMPLIANCE §8 | post-revenue |
| 55 | Concept animations for lesson/exercise content (e.g. the habit-loop diagram, urge rise/crest/pass visual) — currently text/markdown only | PRODUCT_SPEC §5.2 / content pipeline | P |
| 56 | Marketing video/trailer for the App Store listing and launch marketing | Launch prep | L |
| 57 | Content-blocker epic (iOS Screen Time / FamilyControls + ManagedSettings + DeviceActivity, PRODUCT_SPEC §10 roadmap item 2) — requires the privileged `com.apple.developer.family-controls` entitlement; submit the request (main app + every extension bundle ID) as soon as the Apple Developer account exists, approval has lead time | PRODUCT_SPEC §10 | post-v1 |
| 58 | Earn-in daily stakes upgrade (CLINICAL_SPEC §9 item 4b, v1.2): affordable monthly amount + named reward, divided into daily stakes that move into the Reward Jar on session completion; missed-day stakes never earned. Not built in v1 (v1's Commitment Goals ladder is process-weighted credit only, no per-day stake mechanic) | CLINICAL_SPEC §9 | v1.2 |
| 59 | Real-money deposit contracts (CLINICAL_SPEC §9 item 6 / PRODUCT_SPEC §10 roadmap item 4b): external payment flow, charity forfeiture on failed process goals, cool-off period + hardship cancellation. Requires legal + App Store compliance review first | CLINICAL_SPEC §9 / PRODUCT_SPEC §10 | post-v1, legal-gated |

## ✅ Resolved (kept for the record)

| Item | Resolved by |
|------|-------------|
| Supabase client crashed at import with no .env | Fixed in Epic 2b run |
| Banned-words lint false positive on CBT/ACT proper nouns | Lint allowlist + LEGAL_COMPLIANCE §2.1 exception codified |
| PPCS-6 component tags mismatched item order | Remapped in PPCS-6 fill task |
| #14 10-Minute Shift shift-list reconciliation with Week 2 Day 6 | Epic 5b: both now read/write the program store's `exerciseOutputs.shift_list`; the toolkit store's separate copy was removed |
| #11 Evening check-in was lightweight (rotating prompt + free text only) | Epic 6: full mood (5-pt) + urges y/n+count + rotating prompt model in useJournalStore, one system only; old `useProgramStore.checkinResponses` migrated via a one-time, idempotent `migrateLegacyCheckins()` (honest neutral defaults for fields the old model never captured) |
| #30 content/onboarding_insights.json's two entries were TODO(content) placeholders | Epic 8 reconciliation: content/week3.json shipped the authored, cited copy for both (insight-escalation, insight-quits) — merged in, replacing the placeholders. Field shape changed from text/citation to title/body/citation_short to match the authored content exactly; InsightScreen and the OnboardingInsight type updated to match |
| #37 urge_script_written milestone was wired but unverified against real Week 3 content | Epic 8 reconciliation: verified against the real content/week3.json Day 7 payload — and caught a real bug in the process. urge_script is a commitment_builder output (`{statement, signature, signed_at}`), not a bare string; both consumers (progress.tsx's milestone check, and the new Urge Surf entry display) were typed as `<string>` and would have rendered the object directly as a React child, crashing on first real use. Fixed both to read `.statement`. Milestone confirmed unlockable end to end |
| #46 checkin_integration gated on `position.week === 4` specifically | Epic 11: generalized to `getCheckinIntegratedActionKeys()`, a scan for every committed_action_planner payload with `checkin_integration: true` (now Week 4's committed_actions and Week 5's movement_plan, merged) — no week-number hardcoding left |
| #27 Emergency Card UI didn't exist | Epic 12 (Week 6): real screen (`app/(modals)/emergency-card.tsx`) compiled from Week 6 Day 5's `emergency_card_builder` sections in default/user-edited order, reachable from the Toolkit header, the lapse debrief, and Progress. Tool-ranking free text parsed into real tool buttons; "My person" resolved from a nested profile section; hidden sections respected |
| #16 End-of-content messaging ("more content coming soon" past latest week) | Epic 12 (Week 6, final week): `programCompletedAt` flag + Today's maintenance-mode branch (weekly booster rotation, continued check-ins, unchanged re-assessment banner) fully replaces the empty state once Week 6 Day 7 completes |
| #47 `summarizeExerciseOutput` gaps (partial — the two shapes Week 6 actually needed) | Epic 12: added `profile_builder` ({sections}) and `commitment_builder` ({statement,...}) branches, fixing Week 6 Day 4/5's nested sourcing (pattern_profile inside relapse_prevention_plan; urge_script inside the Emergency Card). Not a full speculative audit of every hypothetical future shape — closed the concrete gaps this epic exposed, same reactive pattern as before |
| #12 emergency_card_line's plain-string shape ("worth structuring if Emergency Card needs provenance") | Epic 12: built the real Emergency Card against the plain-string shape as-is — no restructuring was needed; `summarizeExerciseOutput`/the Emergency Card compiler both read it directly |
| #35 Push notification scheduling (daily lesson/evening check-in/risky-window/re-assessment) was unbuilt | Epic 13: `src/lib/notifications/{schedule,riskyWindow,availability,scheduler}.ts` + `useNotificationScheduler` (mounted at the app root) build and apply the real local-notification schedule from `useSettingsStore`/`useProgramStore`/`useToolkitStore`/`useAssessmentHistoryStore`, INC-2 availability-checked; risky-window reminder is a dismissible opt-in offer (`RiskyWindowOffer`, Today tab) gated on Epic 7's practice-excluded cluster logic; maintenance-mode cadence and weekly-booster respect Week 6 Day 6's `maintenance_plan`; any preference change cancels and rebuilds the whole schedule (cancel-all-then-reschedule) |
| #1 Hardcoded `HAS_PRO_ENTITLEMENT = true` in ToolkitHome | Epic 3: `useSubscriptionStore` (`src/features/paywall/`) is the single source of truth for the real "pro" entitlement, backed by `src/lib/revenuecat/{availability,client}.ts` (INC-2 availability-checked, INC-8 no-throw wrappers). ToolkitHome now reads `hasProEntitlement` from the store; Urge Surf/Breather free-forever rule regression-tested independently of entitlement state. Real paywall UI (`app/(onboarding)/paywall.tsx`) fetches the live offering, purchases, and refreshes entitlement; Settings shows live subscription status. RevenueCat configured at app root from `EXPO_PUBLIC_REVENUECAT_IOS_KEY` |
| #50 Emergency Card Tools section could render zero buttons on unparseable `tool_ranking` free text | fix-50-card-fallback: `resolveEmergencyCardToolIds` (emergencyCard.ts) guarantees an action_buttons section never yields an empty tool list — when the free text parses to no known tool (unusual wording, or nothing saved yet) it falls back to the always-free tools (Urge Surf + Breather), derived from the now-exported `FREE_FOREVER_TOOLS` single source of truth so they work regardless of subscription (PRODUCT_SPEC §6 ethical floor). Covered by unit tests (resolver + compile-level) and a render-level test proving the unparseable case shows exactly the two fallback buttons, not raw text |
| #17 Age input was plain DD/MM/YYYY text fields | fix-device-qa-1: replaced with a native date picker (`@react-native-community/datetimepicker`) in `DateOfBirthField`, INC-2 availability-checked via an error-boundary fallback to the DD/MM/YYYY fields (Expo Go / no native module). The age-gate DECISION logic (calculateAge/isMinor, minor→resources redirect) is unchanged and still passing; only the input method changed. The fallback's date validation is a pure, tested helper (`buildDateFromParts`) |
